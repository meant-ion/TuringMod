var ChatBadgeList_1;
import { __decorate } from "tslib";
import { DataObject, rawDataSymbol, rtfm } from '@twurple/common';
import { ChatBadgeSet } from "./ChatBadgeSet.mjs";
/**
 * A list of badge sets.
 */
let ChatBadgeList = ChatBadgeList_1 = class ChatBadgeList extends DataObject {
    /**
     * Names of all badge sets in the list.
     */
    get badgeSetNames() {
        return Object.keys(this[rawDataSymbol]);
    }
    /**
     * Gets a specific badge set by name.
     *
     * @param name The name of the badge set.
     */
    getBadgeSet(name) {
        return new ChatBadgeSet(this[rawDataSymbol][name]);
    }
    /** @private */
    _merge(additionalData) {
        if (additionalData instanceof ChatBadgeList_1) {
            additionalData = additionalData[rawDataSymbol];
        }
        return new ChatBadgeList_1({ ...this[rawDataSymbol], ...additionalData });
    }
};
ChatBadgeList = ChatBadgeList_1 = __decorate([
    rtfm('api', 'ChatBadgeList')
], ChatBadgeList);
export { ChatBadgeList };

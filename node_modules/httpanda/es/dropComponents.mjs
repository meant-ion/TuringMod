export function dropComponents(url, count) {
    while (count--) {
        var index = url.indexOf('/', 1);
        if (index === -1) {
            return '/';
        }
        url = url.substr(index);
    }
    return url;
}
//# sourceMappingURL=dropComponents.mjs.map